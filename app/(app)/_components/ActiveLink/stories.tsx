import { Meta, StoryObj } from '@storybook/react'
import { ActiveLink } from '.'

export default {
  title: 'app/(app)/_components/ActiveLink',
  component: ActiveLink,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}