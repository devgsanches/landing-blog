import { Meta, StoryObj } from '@storybook/react'
import { Badge } from '.'

export default {
  title: 'app/(app)/_components/Badge',
  component: Badge,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}