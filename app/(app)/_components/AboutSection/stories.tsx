import { Meta, StoryObj } from '@storybook/react'
import { AboutSection } from '.'

export default {
  title: 'app/(app)/_components/AboutSection',
  component: AboutSection,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}