import { Meta, StoryObj } from '@storybook/react'
import { Card } from '.'

export default {
  title: 'app/(app)/_components/Card',
  component: Card,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}