import { Meta, StoryObj } from '@storybook/react'
import { BenefitsSection } from '.'

export default {
  title: 'app/(app)/_components/BenefitsSection',
  component: BenefitsSection,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}