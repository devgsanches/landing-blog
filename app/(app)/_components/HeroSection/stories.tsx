import { Meta, StoryObj } from '@storybook/react'
import { HeroSection } from '.'

export default {
  title: 'app/(app)/_components/HeroSection',
  component: HeroSection,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}